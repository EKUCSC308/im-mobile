Aaron
    +UI Theory
        -Color Theory
        -Shape Theory
        -UX Layout
    +Implementation of Labels/Text boxes for sent/recieved messages
    +IRL Time Management (still needs work)
    

Sierra
    +Alerts for incorrect username/password, new user, and new chat options
    +TableView for chats
    +ScrollView implementation for send/recieve messages
        -Open/closing keyboard notifications moves scrollView up/down
        -ScrollView scrolls to bottom when moved up/down
        -ScrollView automatically scrolls to the bottom with new messages
        -Adding labels/text boxes for each message
        -Increasing the size of the scrollView for each new message
        -Adding the textField/ sendButton after each new message

Michael


Jeremy


